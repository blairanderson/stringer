.list-group
  - if user_signed_in?
    = link_to('Logout', destroy_user_session_path, :method => :delete, class: "list-group-item")
  - else
    = link_to('Login', new_user_session_path, class: "list-group-item")

  - if user_signed_in?
    = link_to('Edit registration', edit_user_registration_path, class: "list-group-item")
  - else
    = link_to('Register', new_user_registration_path, class: "list-group-item")
